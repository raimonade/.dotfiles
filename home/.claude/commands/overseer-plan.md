# Overseer Plan Command

Convert markdown planning documents into trackable Overseer task hierarchies.

First, invoke the overseer skill:

```
Skill({ skill: "overseer" })
```

Then convert the provided document into milestones/tasks/subtasks using the Overseer MCP tools.

User request: $ARGUMENTS
