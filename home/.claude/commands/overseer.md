# Overseer Command

Task orchestration via Overseer MCP.

First, invoke the overseer skill:

```
Skill({ skill: "overseer" })
```

Then follow the skill instructions to manage tasks.

User request: $ARGUMENTS
