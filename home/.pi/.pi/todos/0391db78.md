{
  "id": "0391db78",
  "title": "Fix duplicate no-payload warning in /context annotated export",
  "tags": [
    "bug",
    "pi",
    "context-extension"
  ],
  "status": "closed",
  "created_at": "2026-04-05T21:41:07.791Z"
}

Fixed `home/.pi/agent/extensions/context.ts` so `/context` only blocks the **raw** export when no provider payload has been captured yet. The **annotated** export now opens as intended even before the first model request, using its existing no-payload fallback sections. Validated the edited file still loads with `npm exec -- tsx -e "import('./agent/extensions/context.ts').then(() => console.log('ok'))"`.
