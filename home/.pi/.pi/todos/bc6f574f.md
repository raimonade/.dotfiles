{
  "id": "bc6f574f",
  "title": "Install and configure fff-mcp globally for pi via pi-mcp, plus append system guidance",
  "tags": [
    "pi",
    "mcp",
    "fff",
    "config"
  ],
  "status": "closed",
  "created_at": "2026-04-05T21:57:05.888Z"
}

Installed `fff-mcp` globally to `~/.local/bin/fff-mcp`. Enabled the local `pi-mcp` extension from `home/.pi/agent/settings.json`, added an eager `fff` MCP server with `directTools: true` in `home/.pi/agent/mcp.json`, created `home/.pi/agent/APPEND_SYSTEM.md` to prefer `fff_*` tools over bash search commands, bootstrapped the MCP metadata cache via `pi -p`, and verified that pi could call `fff_find_files` as a direct tool.
